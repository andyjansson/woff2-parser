module.exports = function (unicoderange) {
	var ranges = [];
	if (unicoderange & 0x00000001) ranges.push("Basic Latin");
	if (unicoderange & 0x00000002) ranges.push("Latin-1 Supplement");
	if (unicoderange & 0x00000004) ranges.push("Latin Extended-A");
	if (unicoderange & 0x00000008) ranges.push("Latin Extended-B");
	if (unicoderange & 0x00000010) ranges.push("IPA Extensions");
	if (unicoderange & 0x00000020) ranges.push("Spacing Modifier Letters");
	if (unicoderange & 0x00000040) ranges.push("Combining Diacritical Marks");
	if (unicoderange & 0x00000080) ranges.push("Greek and Coptic");
	if (unicoderange & 0x00000100) ranges.push("Coptic");
	if (unicoderange & 0x00000200) ranges.push("Cyrillic");
	if (unicoderange & 0x00000400) ranges.push("Armenian");
	if (unicoderange & 0x00000800) ranges.push("Hebrew");
	if (unicoderange & 0x00001000) ranges.push("Vai");
	if (unicoderange & 0x00002000) ranges.push("Arabic");
	if (unicoderange & 0x00004000) ranges.push("NKo");
	if (unicoderange & 0x00008000) ranges.push("Devanagari");
	if (unicoderange & 0x00010000) ranges.push("Bengali");
	if (unicoderange & 0x00020000) ranges.push("Gurmukhi");
	if (unicoderange & 0x00040000) ranges.push("Gujarati");
	if (unicoderange & 0x00080000) ranges.push("Oriya");
	if (unicoderange & 0x00100000) ranges.push("Tamil");
	if (unicoderange & 0x00200000) ranges.push("Telugu");
	if (unicoderange & 0x00400000) ranges.push("Kannada");
	if (unicoderange & 0x00800000) ranges.push("Malayalam");
	if (unicoderange & 0x01000000) ranges.push("Thai");
	if (unicoderange & 0x02000000) ranges.push("Lao");
	if (unicoderange & 0x04000000) ranges.push("Georgian");
	if (unicoderange & 0x08000000) ranges.push("Balinese");
	if (unicoderange & 0x10000000) ranges.push("Hangul Jamo");
	if (unicoderange & 0x20000000) ranges.push("Latin Extended Additional");
	if (unicoderange & 0x40000000) ranges.push("Greek Extended");
	if (unicoderange & 0x80000000) ranges.push("General Punctuation");
	return ranges;
};