module.exports = function (unicoderange) {
	var ranges = [];
	if (unicoderange & 0x00000001) ranges.push("Buginese");
	if (unicoderange & 0x00000002) ranges.push("Glagolitic");
	if (unicoderange & 0x00000004) ranges.push("Tifinagh");
	if (unicoderange & 0x00000008) ranges.push("Yijing Hexagram Symbols");
	if (unicoderange & 0x00000010) ranges.push("Syloti Nagri");
	if (unicoderange & 0x00000020) ranges.push("Linear B Syllabary");
	if (unicoderange & 0x00000040) ranges.push("Ancient Greek Numbers");
	if (unicoderange & 0x00000080) ranges.push("Ugaritic");
	if (unicoderange & 0x00000100) ranges.push("Old Persian");
	if (unicoderange & 0x00000200) ranges.push("Shavian");
	if (unicoderange & 0x00000400) ranges.push("Osmanya");
	if (unicoderange & 0x00000800) ranges.push("Cypriot Syllabary");
	if (unicoderange & 0x00001000) ranges.push("Kharoshthi");
	if (unicoderange & 0x00002000) ranges.push("Tai Xuan Jing Symbols");
	if (unicoderange & 0x00004000) ranges.push("Cuneiform");
	if (unicoderange & 0x00008000) ranges.push("Counting Rod Numerals");
	if (unicoderange & 0x00010000) ranges.push("Sundanese");
	if (unicoderange & 0x00020000) ranges.push("Lepcha");
	if (unicoderange & 0x00040000) ranges.push("Ol Chiki");
	if (unicoderange & 0x00080000) ranges.push("Saurashtra");
	if (unicoderange & 0x00100000) ranges.push("Kayah Li");
	if (unicoderange & 0x00200000) ranges.push("Rejang");
	if (unicoderange & 0x00400000) ranges.push("Cham");
	if (unicoderange & 0x00800000) ranges.push("Ancient Symbols");
	if (unicoderange & 0x01000000) ranges.push("Phaistos Disc");
	if (unicoderange & 0x02000000) ranges.push("Carian");
	if (unicoderange & 0x04000000) ranges.push("Domino Tiles");
	return ranges;
};